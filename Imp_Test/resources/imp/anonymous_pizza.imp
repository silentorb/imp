namespace test {

	class Pizza {
		toppings:string[]

		add(topping:string) {
			if (topping == null)
				return

			var anon = (topping)=> toppings.add(topping)
			anon(topping)
		}
	}
}