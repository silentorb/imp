namespace outerspace {
	external class Mars {

	}
}

namespace test {

	enum Crust {
		crispy,
		soft
		burnt
		missing
	}

	class Pizza {
		toppings:string[]
		crust:Crust
		variation:bool
		static taste:string

		add(topping:string) {
			var x = 0
			if (topping == null)
				return
			else
				x = 1.5
				

			toppings.add(topping)
			crust = Crust.burnt
		}

		get_eaten():string[] {
			return toppings
		}
	}
}